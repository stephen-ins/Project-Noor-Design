{% extends 'base.html.twig' %}

{% block title %}Noor Design | Profile
{% endblock %}

{# AFFICHAGE DE LA PAGE PROFIL USER OU ADMIN #}

{% block content %}

	<!-- Section profil du client inscrit et connecté -->

	<div>
		<!-- Page Header -->
		<div class="color-bg-header py-16">
			<div class="container mx-auto px-6 text-center">
				<h1 class="text-4xl font-semibold text-gray-800 mb-2">
					Mon espace client
				</h1>
				<p class="text-lg text-gray-600">
					Gestion du compte et de mes commandes
				</p>
			</div>
		</div>

		<!-- Profil Section -->
		<section class="container mx-auto px-6 py-12">
			<div
				class="flex flex-col md:flex-row gap-8">
				<!-- Sidebar Navigation avec mise en place d'un ancrage via id# pour acces direct -->
				<div class="w-full md:w-1/4">
					<div class="bg-white rounded-lg shadow-md p-6">
						<h3 class="text-xl font-semibold text-gray-800 mb-6">Menu</h3>
						<ul class="space-y-3">
							<li>
								<a href="#informations-personnelles" class="flex items-center text-gold font-medium">
									<svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" fill="none" viewbox="0 0 24 24" stroke="currentColor">
										<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
									</svg>
									Mes informations
								</a>
							</li>
							<li>
								<a href="#adresses" class="flex items-center text-gray-600 hover:text-gold transition">
									<svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" fill="none" viewbox="0 0 24 24" stroke="currentColor">
										<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
										<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
									</svg>
									Mes adresses
								</a>
							</li>
							<li>
								<a href="#commandes" class="flex items-center text-gray-600 hover:text-gold transition">
									<svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" fill="none" viewbox="0 0 24 24" stroke="currentColor">
										<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z"/>
									</svg>
									Mes commandes
								</a>
							</li>

							<li>
								<a href="#favoris" class="flex items-center text-gray-600 hover:text-gold transition">
									<svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" fill="none" viewbox="0 0 24 24" stroke="currentColor">
										<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"/>
									</svg>
									Ma Whishlist
								</a>
							</li>
							<li>
								<a href="#securite" class="flex items-center text-gray-600 hover:text-gold transition">
									<svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" fill="none" viewbox="0 0 24 24" stroke="currentColor">
										<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"/>
									</svg>
									Sécurité
								</a>
							</li>
							<li class="pt-4 border-t border-gray-200">
								<a href="{{ path('app_logout') }}" class="flex items-center text-gray-600 hover:text-red-500 transition">
									<svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" fill="none" viewbox="0 0 24 24" stroke="currentColor">
										<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/>
									</svg>
									Se déconnecter
								</a>
							</li>
						</ul>
					</div>
				</div>

				<!-- Main Content -->
				<div
					class="w-full md:w-3/4">
					{# Messages de succès #}
					{% for message in app.flashes('success') %}
						<div class="bg-green-50 border border-green-400 text-green-800 px-4 py-3 rounded-md shadow-sm mb-4" role="alert">
							<div class="flex items-center">
								<svg class="h-5 w-5 text-green-500 mr-2" xmlns="http://www.w3.org/2000/svg" fill="none" viewbox="0 0 24 24" stroke="currentColor">
									<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
								</svg>
								<span class="text-sm font-medium">{{ message }}</span>
							</div>
						</div>
					{% endfor %}

					{# Messages d'erreur #}
					{% for message in app.flashes('error') %}
						<div class="bg-red-50 border border-red-400 text-red-700 px-4 py-3 rounded-md shadow-sm mb-4" role="alert">
							<div class="flex items-center">
								<svg class="h-5 w-5 text-red-500 mr-2" xmlns="http://www.w3.org/2000/svg" fill="none" viewbox="0 0 24 24" stroke="currentColor">
									<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/>
								</svg>
								<span class="text-sm font-medium">{{ message }}</span>
							</div>
						</div>
					{% endfor %}

					<!-- Informations personnelles -->
					<div id="informations-personnelles" class="bg-white rounded-lg shadow-md p-8 mb-8">
						<div class="flex justify-between items-center mb-6">
							<h2 class="text-2xl font-semibold text-gray-800">
								Mes informations personnelles
							</h2>
							<a href="{{ path('app_profile_edit') }}" class="text-gold hover:text-gold-dark transition focus:outline-none">
								<svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewbox="0 0 24 24" stroke="currentColor">
									<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.232 5.232l3.536 3.536m-2.036-5.036a2.5 2.5 0 113.536 3.536L6.5 21.036H3v-3.572L16.732 3.732z"/>
								</svg>
							</a>
						</div>

						<div class="grid grid-cols-1 md:grid-cols-2 gap-6">
							<div>
								<p class="text-sm text-gray-600 mb-1">Prénom</p>
								<p class="text-lg font-medium">{{ user.prenom }}</p>
							</div>
							<div>
								<p class="text-sm text-gray-600 mb-1">Nom</p>
								<p class="text-lg font-medium">{{ user.nom }}</p>
							</div>
							<div>
								<p class="text-sm text-gray-600 mb-1">Email</p>
								<p class="text-lg font-medium">{{ user.email }}</p>
							</div>
							<div>
								<p class="text-sm text-gray-600 mb-1">Date de naissance</p>
								<p class="text-lg font-medium">{{ user.birthday|date('d/m/Y') }}</p>
							</div>
							<div>
								<p class="text-sm text-gray-600 mb-1">Genre</p>
								<p class="text-lg font-medium">{{ user.genre.value }}</p>
							</div>
							<div>
								<p class="text-sm text-gray-600 mb-1">Téléphone</p>
								<p class="text-lg font-medium">{{ user.phone }}</p>
							</div>
						</div>
					</div>

					<!-- Adresse de livraison -->
					<div id="adresses" class="bg-white rounded-lg shadow-md p-8 mb-8">
						<div class="flex justify-between items-center mb-6">
							<h2 class="text-2xl font-semibold text-gray-800">
								Mon adresse de livraison
							</h2>
							{# Lien vers page de modification de la fiche client #}
							<a href="{{ path('app_profile_edit') }}" class="text-gold hover:text-gold-dark transition focus:outline-none">
								<svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewbox="0 0 24 24" stroke="currentColor">
									<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.232 5.232l3.536 3.536m-2.036-5.036a2.5 2.5 0 113.536 3.536L6.5 21.036H3v-3.572L16.732 3.732z"/>
								</svg>
							</a>
						</div>

						<div class="border border-gray-200 rounded-lg p-6">
							<div class="mb-4">
								<p class="text-lg font-medium mb-1">{{ user.prenom }}
									{{ user.nom }}</p>
								<p class="text-gray-600">{{ user.adresse }}</p>
								<p class="text-gray-600">{{ user.zipcode }}
									{{ user.ville }}</p>
								<p class="text-gray-600">{{ user.pays }}</p>
							</div>
							<div class="flex space-x-4">
								<span class="inline-block bg-gold bg-opacity-10 text-white text-xs px-3 py-1 rounded-full">Adresse principale</span>
							</div>
						</div>

						{# Mesures conservatoire pour ajout d'une nouvelle adresse de livraison #}
						{# <div class="mt-4">
																																																	<button class="flex items-center text-gold hover:text-gold-dark">
																																																		<svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-1" fill="none" viewbox="0 0 24 24" stroke="currentColor">
																																																			<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"/>
																																																		</svg>
																																																		Ajouter une nouvelle adresse
																																																	</button>
																																																</div> #}
					</div>

					<!-- Historique de commandes -->
					<div id="commandes" class="bg-white rounded-lg shadow-md p-8 mb-8">
						<h2 class="text-2xl font-semibold text-gray-800 mb-6">
							Mes commandes récentes
						</h2>

						{% if user.orders is empty %}
							<div class="text-center py-8">
								<p class="text-gray-600">Vous n'avez pas encore de commande.</p>
							</div>
						{% else %}
							{% for order in user.orders|slice(0, 5) %}
								<div class="border-b border-gray-200 pb-2 mb-4">
									<div class="flex justify-between items-center mb-2">
										<div>
											<span class="font-medium">Commande #{{ order.orderNumber }}</span>
											<span class="text-sm text-gray-500 ml-2">{{ order.orderDate|date('d/m/Y') }}</span>
										</div>
										<span class="px-3 py-1 bg-{{ order.status == 'LIVREE' ? 'green' : 'blue' }}-100 text-{{ order.status == 'LIVREE' ? 'green' : 'blue' }}-800 text-xs rounded-full">
											{{ order.status.value }}
										</span>
									</div>
									<div class="flex justify-between text-sm">
										<span class="text-gray-600">{{ order.orderDetails|length }}
											article(s)</span>
										<span class="font-medium">{{ order.amount|number_format(2, ',', ' ') }}
											€</span>
									</div>
									<div class="mt-2">
										<a href="#" class="text-gold text-sm hover:underline">Voir les détails</a>
									</div>
								</div>
							{% endfor %}

							{% if user.orders|length > 5 %}
								<div class="text-center mt-6">
									<a href="#" class="text-gold hover:underline">Voir toutes mes commandes</a>
								</div>
							{% endif %}
						{% endif %}
					</div>

					<!-- Mes favoris -->
					<div id="favoris" class="bg-white rounded-lg shadow-md p-8 mb-8">
						<h2 class="text-2xl font-semibold text-gray-800 mb-6">
							Ma Whishlist
						</h2>
						{# Boucle de whishlist - favoris #}
						<div class="grid grid-cols-1 md:grid-cols-2 gap-4">
							<div class="flex border border-gray-200 rounded-lg overflow-hidden">
								<div class="w-1/3">
									<img src="../assets/images/placeholder-product.jpg" alt="Collier en or" class="w-full h-full object-cover"/>
								</div>
								<div class="w-2/3 p-4 flex flex-col justify-between">
									<div>
										<h3 class="font-medium">Collier Lumière dorée</h3>
										<p class="text-gold text-sm mt-1">45,00 €</p>
									</div>
									<div class="flex justify-between items-center mt-3">
										<button class="text-white bg-gold px-3 py-1 rounded-full text-xs hover:bg-gold-dark transition">
											Ajouter au panier
										</button>
										<button class="text-gray-400 hover:text-red-500">
											<svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="currentColor" viewbox="0 0 24 24" stroke="currentColor">
												<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"/>
											</svg>
										</button>
									</div>
								</div>
							</div>
						</div>

						<div class="text-center mt-6">
							<a href="{{path('app_catalogue')}}" class="inline-block px-4 py-2 border border-gold text-gold rounded-full hover:bg-gold hover:text-white transition">
								Découvrir plus de bijoux
							</a>
						</div>
					</div>

					<!-- Sécurité -->
					<div id="securite" class="bg-white rounded-lg shadow-md p-8">
						<h2 class="text-2xl font-semibold text-gray-800 mb-6">
							Paramètres de sécurité
						</h2>

						<div class="mb-8">
							<h3 class="text-lg font-medium mb-4">
								Modifier mon mot de passe
							</h3>
							<form action="{{ path('app_profile_password') }}" method="post" class="space-y-4">
								<input type="hidden" name="_csrf_token" value="{{ csrf_token('update_password') }}">
								<div>
									<label for="current_password" class="block text-sm font-medium text-gray-700 mb-1">
										Mot de passe actuel
									</label>
									<input type="password" id="current_password" name="current_password" class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-gold"/>
								</div>
								<div>
									<label for="new_password" class="block text-sm font-medium text-gray-700 mb-1">
										Nouveau mot de passe
									</label>
									<input type="password" id="new_password" name="new_password" class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-gold"/>
									<p class="text-xs text-gray-500 mt-1">
										Minimum 8 caractères, avec au moins une majuscule, une minuscule,
																																																																																																																																																																																																																																																																																							un chiffre et un caractère spécial (@$!#%*?&.,_-)
									</p>
								</div>
								<div>
									<label for="confirm_password" class="block text-sm font-medium text-gray-700 mb-1">
										Confirmer le nouveau mot de passe
									</label>
									<input type="password" id="confirm_password" name="confirm_password" class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-gold"/>
								</div>
								<div>
									<button type="submit" class="px-4 py-2 bg-gold text-white rounded-md hover:bg-gold-dark transition focus:outline-none focus:ring-2 focus:ring-gold focus:ring-opacity-50">
										Mettre à jour le mot de passe
									</button>
								</div>
							</form>
						</div>


						<div class="border-t border-gray-200 pt-6">
							<h3 class="text-lg font-medium mb-4">Supprimer mon compte</h3>
							<p class="text-gray-600 mb-4">
								La suppression de votre compte est définitive. Toutes vos
																																																																																																																données personnelles seront effacées conformément à notre
																																																																																																																politique de confidentialité.
							</p>
							<button type="button" class="px-4 py-2 border border-red-500 text-red-500 rounded-md hover:bg-red-500 hover:text-white transition focus:outline-none" onclick="openDeleteModal()">
								Supprimer mon compte
							</button>
						</div>


						<!-- Modale de confirmation de suppression -->
						<div id="deleteAccountModal" class="fixed inset-0 bg-gray-800 bg-opacity-75 flex items-center justify-center z-50 hidden">
							<div class="bg-white rounded-lg shadow-xl max-w-md w-full mx-4 overflow-hidden">
								<div class="bg-red-50 px-6 py-4 border-b border-red-100">
									<div class="flex items-center justify-between">
										<h3 class="text-lg font-medium text-red-700">
											Confirmation de suppression
										</h3>
										<button type="button" class="text-gray-400 hover:text-gray-500" onclick="closeDeleteModal()">
											<svg class="h-6 w-6" fill="none" viewbox="0 0 24 24" stroke="currentColor">
												<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
											</svg>
										</button>
									</div>
								</div>
								<div class="px-6 py-4">
									<div class="mb-4">
										<p class="text-gray-700 mb-2">Êtes-vous sûr de vouloir supprimer votre compte ?</p>
										<p class="text-gray-600 text-sm">Cette action est irréversible. Toutes vos données personnelles seront effacées.</p>
									</div>
									<form action="{{ path('app_profile_delete') }}" method="post" class="mt-6">
										<div class="mb-4">
											<label for="delete_password" class="block text-sm font-medium text-gray-700 mb-1">
												Pour confirmer, veuillez saisir votre mot de passe actuel
											</label>
											<input type="password" id="delete_password" name="password" class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-red-500" required/>
										</div>
										<div class="flex justify-end space-x-4">
											<button type="button" class="px-4 py-2 border border-gray-300 rounded-md text-gray-700 hover:bg-gray-100 transition focus:outline-none focus:ring-2 focus:ring-gold focus:ring-opacity-50" onclick="closeDeleteModal()">
												Annuler
											</button>
											<button type="submit" class="px-4 py-2 bg-red-600 text-white rounded-md hover:bg-red-700 transition focus:outline-none focus:ring-2 focus:ring-red-500 focus:ring-opacity-50">
												Confirmer la suppression
											</button>
										</div>
									</form>
								</div>
							</div>
						</div>


					</div>
				</div>
			</div>
		</section>
	</div>

{% endblock %}

{% block javascripts %}
	{{ parent() }}
	<script>
		function openDeleteModal() {
document.getElementById('deleteAccountModal').classList.remove('hidden');
document.body.classList.add('overflow-hidden');
}

function closeDeleteModal() {
document.getElementById('deleteAccountModal').classList.add('hidden');
document.body.classList.remove('overflow-hidden');
}

// S'assurer que le DOM est chargé avant d'ajouter les événements
document.addEventListener('DOMContentLoaded', function () { // Vérifier si la modale et le bouton existent
const deleteModal = document.getElementById('deleteAccountModal');
const openModalButton = document.querySelector('[onclick="openDeleteModal()"]');
const closeModalButtons = document.querySelectorAll('[onclick="closeDeleteModal()"]');

if (deleteModal && openModalButton) {
openModalButton.addEventListener('click', openDeleteModal);
}

if (closeModalButtons.length > 0) {
closeModalButtons.forEach(button => {
button.addEventListener('click', closeDeleteModal);
});
}
});
	</script>
{% endblock %}
